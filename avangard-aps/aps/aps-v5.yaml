# Avangard APS v5 — основной конфигурационный файл
version: "5.0"
name: avangard-aps

paths:
  schema: schema/
  rules: rules/
  checks: checks/
  validators: validators/
  actions: actions/
  tests: tests/
  knowledge: ../knowledge/
  templates: ../templates/

settings:
  locale: ru-RU
  timezone: Europe/Moscow
